{
  "name": "forge-ui",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@changesets/cli": "^2.27.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
